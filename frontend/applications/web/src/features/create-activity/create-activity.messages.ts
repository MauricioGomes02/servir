import { defineLocalizedMessages } from '@/shared/i18n';
export const createActivityMessages = defineLocalizedMessages({
  'pt-BR': { fallbackError: 'Não foi possível criar a atividade.' },
  'en-US': { fallbackError: 'The activity could not be created.' },
});
