import { defineLocalizedMessages } from '@/shared/i18n';

export const createOrganizationMessages = defineLocalizedMessages({
  'pt-BR': { fallbackError: 'Não foi possível conectar à API.' },
  'en-US': { fallbackError: 'The API could not be reached.' },
});
