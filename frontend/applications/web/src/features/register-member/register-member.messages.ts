import { defineLocalizedMessages } from '@/shared/i18n';
export const registerMemberMessages = defineLocalizedMessages({
  'pt-BR': { fallbackError: 'Não foi possível cadastrar o membro.' },
  'en-US': { fallbackError: 'The member could not be registered.' },
});
