import { defineLocalizedMessages } from '@/shared/i18n';

export const createMinistryMessages = defineLocalizedMessages({
  'pt-BR': { fallbackError: 'Não foi possível criar o ministério.' },
  'en-US': { fallbackError: 'The ministry could not be created.' },
});
