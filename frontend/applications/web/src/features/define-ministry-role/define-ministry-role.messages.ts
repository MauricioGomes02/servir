import { defineLocalizedMessages } from '@/shared/i18n';

export const defineMinistryRoleMessages = defineLocalizedMessages({
  'pt-BR': { fallbackError: 'Não foi possível criar a função ministerial.' },
  'en-US': { fallbackError: 'The ministry role could not be created.' },
});
