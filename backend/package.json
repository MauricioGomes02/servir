{
  "name": "servir",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "applications/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "dev:api": "npm run dev --workspace @servir/api",
    "start:api": "npm run start --workspace @servir/api"
  }
}
